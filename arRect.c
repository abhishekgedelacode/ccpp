#include<stdio.h>

int main() {
	int len, wid;

	printf("enter the length  of the rectangle: ");
	scanf("%d", &len);
	printf("enter the width of the rectangle: ");
	scanf("%d", &wid);

	printf("\n area of the given rectangle is : %d\n", len*wid);

	return 0;
}
